name: Build and Deploy to Client
on: [push]
  

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy app to admin
        uses: appleboy/ssh-action@v0.1.2
        with:
          host: ${{ secrets.CLIENT_HOST }} # server IP address
          key: ${{ secrets.CLIENT_KEY }} # key for server
          username: ${{ secrets.SSH_USERNAME }} # username for server
          passphrase: ${{ secrets.USER_PASSPHRASE }} # passphrase for admin side

          script: |
            cd ~/test
            ssh-keyscan -H ${{ secrets.CLIENT_HOST }} > ~/.ssh/known_hosts # gather SSH public keys from servers
            # git clone git@github.com:cyberGuardianSeniorDesign/cyber-guardian-backend.git
            git clone https://github.com/cyberGuardianSeniorDesign/cyber-guardian-backend.git
            echo 'Successful deployment to Digital Ocean'
